import { styled } from "@linaria/react";
import { Form, Input as InputAntd } from "antd";

export const Container = styled(Form.Item)``

export const Input = styled(InputAntd)``

export const Password = styled(InputAntd.Password)``

export const TextArea = styled(InputAntd.TextArea)``